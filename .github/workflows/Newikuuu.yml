name: Ikuu New Checkin

on:
  schedule:
    # 每天北京时间上午 9 点运行 (UTC 1:00)
    - cron: '0 1 * * *'
  workflow_dispatch:

jobs:
  ikuuu_checkin:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          # 如果 requirements.txt 存在则安装，否则忽略
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
          # 强制安装 requests，防止缺失
          pip install requests

      - name: Run Ikuuu Checkin
        env:
          # 确保你在 Secrets 里配置的是 IKUUU_EMAIL 和 IKUUU_PASSWORD
          IKUUU_EMAIL: ${{ secrets.IKUUU_EMAIL }}
          IKUUU_PASSWORD: ${{ secrets.IKUUU_PASSWORD }}
        # 👇【重要】这里必须改成你新建的文件名 Newikuuu.py
        run: python Newikuuu.py
